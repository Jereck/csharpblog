<% include partials/header %>

    <div class="container">
        <% if(currentUser){ %>
            <div class="card">
                <div class="card-header">
                    Want to create a new post?
                </div>
                <div class="card-body">
                    <a href="/blog/new" class="btn btn-primary">New Post</a>
                </div>
            </div>
        <% } %>
        <div class="custom-container">
            <% blogPost.forEach(function(blogPost){ %>
                <div class="row"> 
                    <div class="col-md-8 blog-posting">
                        <h2><%= blogPost.title %></h2>
                        <h4>By <%= blogPost.author.username %></h4>
                        <hr />
                        <h5><em><%= blogPost.created.toDateString() %></em></h5>
                        <hr />
                        <p>
                            <%- blogPost.content.substring(0, 400) %> ...
                        </p>
                        <a href="/blog/<%= blogPost._id %>" class="btn btn-primary">More</a>
                        <% if(currentUser){ %>
                        <form class="delete-button" action="/blog/<%= blogPost._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-outline-danger">Delete</button>
                        </form>
                        <% } %>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

<% include partials/footer %>


